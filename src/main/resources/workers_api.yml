openapi: 3.0.3
info:
  title: Workers API - OpenAPI 3.0.3
  description: |-
    API to create, update and delete workers. A worker resource represents any employee, contractor or any kind of human capital.
  version: 1.0.0
paths:
  /workers/status:
    get:
      tags:
        - status
      operationId: getWorkersStatus
      responses:
        '200':
          description: successful operation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkersStatus'

  /workers:
    get:
      tags:
        - workers
      operationId: getWorkers
      responses:
        '200':
          description: successful operation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Workers'
  /workers/{id}:
    get:
      tags:
        - worker
      operationId: getWorkerById
      parameters:
        - name: id
          in: path
          description: ID of worker
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: successful operation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Worker'
        '400':
          description: Invalid ID supplied
        '404':
          description: Worker not found
components:
  schemas:
    WorkersStatus:
      required:
        - isActiveFullTimePresent
      type: object
      properties:
        isActiveFullTimePresent:
          type: boolean
          description: Indicates if at least one active full time worker is in talent pool
    Worker:
      required:
        - id
      type: object
      properties:
        id:
          type: integer
          format: int64
          example: 18347239
        name:
          type: string
          example: first name last name
        income:
          type: integer
          description: The annual income for the worker
        type:
          type: string
          description: worker type i.e. full time, contractor, intern
          enum:
            - full time
            - contractor
            - intern
        status:
          type: string
          description: worker status
          enum:
            - active
            - inactive
    Workers:
      type: object
      properties:
        workers:
          type: array
          items:
            $ref: '#/components/schemas/Worker'
          uniqueItems: true